[package]
name = "runner"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
ace-http = { path = "../http" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
