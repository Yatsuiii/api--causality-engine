[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
runner = { path = "../runner" }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
