[package]
name = "ace-core"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
ace-http = { path = "../http" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
rand = "0.8"
chrono = "0.4"
